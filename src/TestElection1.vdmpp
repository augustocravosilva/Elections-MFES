class TestElection1 is subclass of MyTestCase

operations

	-- Entry point that runs all tests with valid inputs
  --public testAll: () ==> ()
  	--testAll() == (
  		--testVoteScn();
  --);


end TestElection1